---
description: 基于 OpenSpec change 生成 Prometheus 实施计划
---

# /opsx-plan - OpenSpec Change → Prometheus Plan

将 OpenSpec 生成的 artifacts 转换为 Prometheus 实施计划。

## 上下文注入

当执行此命令时，系统自动注入：

**命令目的**：为 change 生成可执行的计划，为后续 `/work-start` 或 `/work-new` 做准备。

**关键文件**：
- `openspec/config.yaml` — 项目 context 和 rules
- `openspec/changes/<name>/` — artifacts（proposal/specs/design/tasks）
- `AGENTS.md` — 项目编码规范和技术栈

**工具调用规范**：
```bash
# 获取 change 状态
openspec status --change <name> --json

# 获取 apply 指令（含 tasks 和 contextFiles）
openspec instructions apply --change <name> --json

# 解析 JSON
jq '.progress.complete'  # 完成数
jq '.progress.total'    # 总数
jq '.state'             # blocked/ready/all_done
jq '.contextFiles'      # 需要读取的文件
```

**计划文件位置**：`.sisyphus/plans/<change-name>.md`

---

## 语法

```
/opsx-plan <change-name>
```

或不带参数，系统会列出所有 active changes 供选择。

## 流程总览

```
┌─────────────────────────────────────────────────────────────┐
│  /opsx-propose <name>                                       │
│       ↓                                                     │
│  /opsx-plan <name>                                          │
│       ↓                                                     │
│  读取 artifacts + config + AGENTS.md                        │
│       ↓                                                     │
│  调用 Prometheus 生成计划                                     │
│       ↓                                                     │
│  自动 git commit (.sisyphus + openspec)                      │
│       ↓                                                     │
│  输出：计划文件路径 + 下一步建议                              │
└─────────────────────────────────────────────────────────────┘
```

## 详细流程

### 1. 确定 change 名称

- 如果提供 `<change-name>`：直接使用
- 如果未提供：`openspec list --json` 获取所有 active changes，选择一个

### 2. 获取 OpenSpec 状态

```bash
openspec status --change "<name>" --json
openspec instructions apply --change "<name>" --json
```

解析：
- `schemaName`: 使用的 schema
- `state`: blocked / ready / all_done
- `progress`: { total, complete, remaining }
- `tasks`: 任务列表
- `contextFiles`: 需要读取的文件

### 3. State 分支处理

**blocked**: 提示补全 artifacts
```
❌ 无法生成计划：缺少 artifacts

请先补全：
- proposal.md
- specs/*.md
- design.md
- tasks.md

建议：/opsx-propose 或 /opsx-continue-change
```

**all_done**: 提示已完成
```
✅ 所有任务已完成，无需生成新计划。

建议：/work-done 检查状态
```

**ready**: 继续生成计划

### 4. 读取上下文

根据 `contextFiles` 读取：
- `proposal.md`
- `specs/*.md`
- `design.md`
- `tasks.md`

同时读取：
- `openspec/config.yaml` — 获取 context + rules
- `AGENTS.md` — 获取项目规范

### 5. 生成 Prometheus 计划

调用 Prometheus 生成计划到 `.sisyphus/plans/<change-name>.md`。

计划文件必须包含：

```markdown
## 任务执行时状态更新

每个任务完成后，必须立即更新 `openspec/changes/<name>/tasks.md`：
- 将 `- [ ] 任务描述` 改为 `- [x] 任务描述`

使用 sed 修改：
```bash
sed -i 's/- \[ \]<任务描述>/- [x] <任务描述>/' openspec/changes/<name>/tasks.md
```

## 验证点

每次任务完成后：
- `cmake --build build -j$(nproc)` - 编译通过
- `ctest --output-on-failure` - 测试通过
```

### 6. 自动 Git Commit

生成计划后，自动 commit 到 git：

```bash
# 检查是否有变更
git status --porcelain

# 如果有变更，commit
git add .sisyphus/plans/<name>.md
git add openspec/changes/<name>/
git commit -m "Plan: <name>

- Generated by /opsx-plan
- Schema: <schema-name>
- Tasks: <N> pending
"

# 如果是首次创建 change branch，也需要 commit branch
git branch -u openspec/<name> || git branch openspec/<name>
```

### 7. 输出

```
✅ 计划生成完成！

Change: <name>
Schema: <schema-name>
计划文件: .sisyphus/plans/<name>.md
Tasks: N pending
Git: 已提交

下一步：
  方式 A (串行): /work-start <name>
  方式 B (并行): /work-new <name>
```

---

## 与其他命令的关系

| 前置命令 | 后置命令 |
|----------|----------|
| `/opsx-propose <name>` | `/opsx-plan <name>` |
| `/opsx-plan <name>` | `/work-start <name>` 或 `/work-new <name>` |

---

## Guardrails

1. **State 检查**：`blocked` 时不生成计划，先补全 artifacts
2. **Git 状态**：确保计划文件正确 commit
3. **覆盖保护**：重新执行 `/opsx-plan` 会覆盖旧计划，需确认

---

## 依赖

- `openspec` CLI
- Prometheus (Plan Agent)
- `git`