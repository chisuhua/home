apt update
    2  apt install default-jdk
    4  apt-get install -y curl gnupg build-essential pkg-config libx11-dev libxkbfile-dev libsecret-1-dev
    5  curl -fsSL https://code-server.dev/install.sh | sh
   18  apt install cmake-curses-gui
   21  apt install clang  lldb lld clangd
