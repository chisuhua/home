# My AI Dev Environment

A unified Docker environment for:
- Python + PyTorch (GPU-ready)
- C++/CUDA development
- Node.js (via NVM)
- OpenClaw / OpenCode
- Neovim

## Build & Push (on CPU ECS in Shanghai)

```bash
chmod +x build.sh
chmod +x run_build.sh
./run_build.sh
